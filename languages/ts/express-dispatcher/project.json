{
  "name": "express-dispatcher",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "languages/ts/express-dispatcher/src",
  "projectType": "library",
  "targets": {
    "benchmark": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jiti buildBenchmarks.ts && node benchmark/dist/main.cjs",
        "cwd": "languages/ts/express-dispatcher"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "unbuild",
        "cwd": "languages/ts/express-dispatcher"
      }
    },
    "publish": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm publish",
        "cwd": "languages/ts/express-dispatcher"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm eslint languages/ts/express-dispatcher"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/src"],
      "options": {
        "command": "vitest run . --passWithNoTests --globals",
        "cwd": "languages/ts/express-dispatcher"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "languages/ts/express-dispatcher"
      }
    }
  },
  "tags": []
}
