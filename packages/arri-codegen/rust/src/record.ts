import { type SchemaFormValues } from "arri-codegen-utils";
import { type RustProperty, type GeneratorContext } from "./common";

export function rustHashmapFromSchema(
    schema: SchemaFormValues,
    context: GeneratorContext,
): RustProperty {}
